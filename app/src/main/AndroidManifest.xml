<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.tripmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SignUpActivity" />
        <activity android:name=".activity.EditTripActivityActivity" />
        <activity android:name=".activity.AddTripActivityActivity" />
        <activity android:name=".activity.TripActivitiesListActivity" />
        <activity android:name=".activity.AllActivitiesActivity" />
        <activity android:name=".activity.AddTripActivity" />
        <activity android:name=".activity.GuestsListActivity" />
        <activity android:name=".activity.HomeActivity"
            android:windowSoftInputMode="adjustPan"/>
    </application>

</manifest>